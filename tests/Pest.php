<?php

/*
|--------------------------------------------------------------------------
| Test Bootstrap
|--------------------------------------------------------------------------
|
| These tests assert the cross-boundary contract between the native bridge
| (Kotlin/Swift) and the PHP event layer. They do not require a booted Laravel
| application — only the plugin's own classes (autoloaded via composer) — so
| they run fast under `vendor/bin/pest` once `composer install` has run.
|
*/
